export enum SortDirection {
   Ascending = "ASCENDING", Descending = "DESCENDING",
}

export enum SortType {
   Alphabetic = 'ALPHABETIC', Numeric = 'NUMERIC', None = 'NONE'
}

export enum FilterType {
   Featured = 'Featured', Bestseller = 'Bestseller'
}